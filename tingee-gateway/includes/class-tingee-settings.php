<?php
/**
 * Các field cấu hình của Tingee Gateway trong WooCommerce Settings.
 *
 * File này sẽ được triển khai đầy đủ ở Giai đoạn 3 (Task T3.2).
 * Hiện tại chỉ khai báo class rỗng để plugin có thể kích hoạt mà không lỗi.
 *
 * @package Tingee_Gateway
 */

// Ngăn truy cập trực tiếp.
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * Class Tingee_Settings
 *
 * Chịu trách nhiệm định nghĩa tất cả các form field trong trang cấu hình:
 * bật/tắt, tiêu đề, mô tả, client id, secret token, chọn tài khoản/VA, v.v.
 *
 * Sẽ triển khai đầy đủ ở Task T3.2.
 */
class Tingee_Settings {
	// Nội dung sẽ được thêm vào ở Giai đoạn 3.
}
